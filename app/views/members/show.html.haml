= render 'shared/header'

.head-content-show.main-content
  = @member.nickname
  = link_to member_path, method: :delete, class: 'member-destroy-btn' do
    %button.member-destroy-btn
      メンバーを削除


.main-content-show
  - if @score.length == 0 && user_signed_in?
    .no-data
      前のページからスコアを追加してください

  - if user_signed_in? && @score.length >= 1
    %table
      %thead
        %tr
          %th
          = render 'shared/score-header'

      = render 'shared/members-show'

      %tfoot
        %tr
          %td
            合計
          = render 'shared/score-footer'
